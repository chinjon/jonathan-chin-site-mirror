{
  "name": "jonathan-chin-site",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "11ty:build": "npx @11ty/eleventy",
    "11ty:watch": "npx @11ty/eleventy --watch --quiet --ignore-initial",
    "build": "11ty:build",
    "start": "npm-run-all --parallel 11ty:watch wrangler:dev",
    "wrangler:dev": "npx wrangler dev --live-reload",
    "wrangler:deploy": "npx wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-img": "^6.0.4"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "wrangler": "^4.105.0"
  }
}
